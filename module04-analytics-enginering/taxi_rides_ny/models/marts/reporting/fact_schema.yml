version: 2

models:
  - name: fact_trip
    columns:
      - name: trip_id
        description: "Primary key generated via dbt_utils.generate_surrogate_key"
        tests:
          # - unique
          - not_null